---
import DocsLayout from '../../../layouts/DocsLayout.astro';
const lang = 'zh';
---

<DocsLayout lang={lang}>
  <h1 class="font-mono text-3xl font-bold text-mono-50">CLI</h1>
  <p class="mt-4 text-mono-400 leading-relaxed">
    <code class="text-mono-200">roamy</code> CLI 是一个终端会话管理器，通过 Unix 域套接字（本地）或 WebSocket（远程）与 roamyd 通信。
  </p>

  <h2 class="mt-10 font-mono text-xl font-bold text-mono-100">命令</h2>
  <pre class="mt-4 overflow-x-auto rounded-lg border border-mono-800 bg-mono-900/50 p-4 font-mono text-sm text-mono-300"><code>roamy &lt;program&gt; [args...]     在托管会话中启动程序
roamy shell                   启动默认 Shell ($SHELL)
roamy list                    列出活跃会话
roamy join &lt;session-id&gt;       重新连接到已有会话
roamy kill &lt;session-id&gt;       终止会话
roamy daemon start|stop|status  管理 roamyd 守护进程
roamy update [--open]         检查更新（--open 打开浏览器）
roamy help                    显示帮助
roamy version                 显示版本</code></pre>

  <h2 class="mt-10 font-mono text-xl font-bold text-mono-100">会话生命周期</h2>
  <ol class="mt-4 list-decimal space-y-2 pl-6 text-sm text-mono-400">
    <li><strong class="text-mono-200">创建</strong> &mdash; <code class="text-mono-200">roamy shell</code> 或 <code class="text-mono-200">roamy &lt;program&gt;</code> 通过 roamyd 创建新的 PTY 会话。</li>
    <li><strong class="text-mono-200">连接</strong> &mdash; 终端进入原始模式，代理 I/O 到会话。连接时重放 scrollback。</li>
    <li><strong class="text-mono-200">脱离</strong> &mdash; 按 <kbd class="rounded border border-mono-700 bg-mono-800 px-1.5 py-0.5 text-xs text-mono-300">Ctrl+]</kbd> 然后 <kbd class="rounded border border-mono-700 bg-mono-800 px-1.5 py-0.5 text-xs text-mono-300">d</kbd>。会话继续在后台运行。</li>
    <li><strong class="text-mono-200">重连</strong> &mdash; <code class="text-mono-200">roamy join &lt;id&gt;</code> 重连到会话并重放 scrollback。</li>
    <li><strong class="text-mono-200">销毁</strong> &mdash; <code class="text-mono-200">roamy kill &lt;id&gt;</code> 终止 PTY 进程并删除会话。</li>
  </ol>

  <h2 class="mt-10 font-mono text-xl font-bold text-mono-100">守护进程管理</h2>
  <pre class="mt-4 overflow-x-auto rounded-lg border border-mono-800 bg-mono-900/50 p-4 font-mono text-sm text-mono-300"><code>roamy daemon start   # 在后台启动 roamyd
roamy daemon stop    # 向 roamyd 发送关闭信号
roamy daemon status  # 检查 roamyd 是否运行</code></pre>
  <p class="mt-3 text-sm text-mono-400">
    执行 <code class="text-mono-200">daemon start</code> 时，CLI 查找 <code class="text-mono-200">roamyd</code> 二进制（与 <code class="text-mono-200">roamy</code> 同目录或 PATH 中），以 <code class="text-mono-200">--daemon</code> 参数启动。守护进程启动后会执行非阻塞更新检查。
  </p>

  <h2 class="mt-10 font-mono text-xl font-bold text-mono-100">连接方式</h2>
  <p class="mt-3 text-sm text-mono-400">
    CLI 在 Unix 上通过 Unix 域套接字（<code class="text-mono-200">$XDG_RUNTIME_DIR/roamy/roamyd.sock</code> 或 <code class="text-mono-200">ROAMY_SOCKET</code>）连接 roamyd。在 Windows 上通过 TCP WebSocket 端点连接。
  </p>

  <h2 class="mt-10 font-mono text-xl font-bold text-mono-100">环境变量</h2>
  <div class="mt-4 overflow-x-auto">
    <table class="w-full text-sm text-mono-400">
      <thead><tr class="border-b border-mono-800 text-left"><th class="pb-2 pr-6 font-mono text-mono-200">变量</th><th class="pb-2 font-mono text-mono-200">说明</th></tr></thead>
      <tbody class="divide-y divide-mono-800/50">
        <tr><td class="py-2 pr-6 font-mono text-mono-100"><code>ROAMY_SOCKET</code></td><td class="py-2">UDS 套接字路径（默认：自动检测，仅 Unix）</td></tr>
        <tr><td class="py-2 pr-6 font-mono text-mono-100"><code>SHELL</code></td><td class="py-2"><code class="text-mono-200">roamy shell</code> 使用的默认 Shell</td></tr>
      </tbody>
    </table>
  </div>
</DocsLayout>
